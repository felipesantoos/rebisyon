<% content_for :title, "New preset" %>
<% content_for :page_title, "New Preset" %>

<div class="space-y-6">
  <div>
    <h1 class="text-2xl font-semibold text-gray-900">New preset</h1>
    <p class="mt-1 text-sm text-gray-500">Create a new deck options preset.</p>
  </div>

  <div class="card">
    <%= render "form", deck_options_preset: @preset %>
  </div>

  <div>
    <%= link_to "Back to presets", deck_options_presets_path, class: "text-gray-600 hover:text-gray-900" %>
  </div>
</div>
