<% content_for :title, "Edit Note" %>
<% content_for :page_title, "Edit Note" %>

<div class="max-w-4xl">
  <div class="mb-6">
    <h1 class="text-2xl font-semibold text-gray-900">Edit Note</h1>
    <p class="mt-1 text-sm text-gray-500">Update your note.</p>
  </div>
  
  <div class="card">
    <%= render "form", note: @note, note_types: @note_types, decks: @decks %>
  </div>
</div>
