<% content_for :title, "Add Note" %>
<% content_for :page_title, "Add Note" %>

<div class="max-w-4xl">
  <div class="mb-6">
    <h1 class="text-2xl font-semibold text-gray-900">Add New Note</h1>
    <p class="mt-1 text-sm text-gray-500">Create a new note for your collection.</p>
  </div>
  
  <div class="card">
    <%= render "form", note: @note, note_types: @note_types, decks: @decks %>
  </div>
</div>
